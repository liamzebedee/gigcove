db:
  image: orchardup/postgresql
  ports:
    - "5432"
web:
  build: .
  command: "bundle exec rackup -p 3000"
  volumes:
    - .:/railsapp
  ports:
    - "3000:3000"
  links:
    - db
  environment:
    RACK_ENV: development
    SECRET_KEY_BASE:
  dns: 127.0.0.1
